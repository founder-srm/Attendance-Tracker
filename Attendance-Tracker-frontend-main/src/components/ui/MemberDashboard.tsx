import { useState } from "react";  // ΓöÇΓöÇ Mock Data (replace with Supabase calls) ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ const MOCK_USER = {   id: "user-001",   name: "Richa",   email: "richa@foundersclub.org",   role: "Member",   joinedAt: "2024-08-01",   avatar: null, };  const MOCK_ATTENDANCE = [   { id: 1,  meeting: "Weekly Sync #1",  date: "2025-01-06", status: "present", duration: 60  },   { id: 2,  meeting: "Product Review",  date: "2025-01-13", status: "present", duration: 90  },   { id: 3,  meeting: "Weekly Sync #2",  date: "2025-01-20", status: "absent",  duration: 60  },   { id: 4,  meeting: "Strategy Meet",   date: "2025-01-27", status: "present", duration: 120 },   { id: 5,  meeting: "Weekly Sync #3",  date: "2025-02-03", status: "present", duration: 60  },   { id: 6,  meeting: "Design Sprint",   date: "2025-02-10", status: "absent",  duration: 180 },   { id: 7,  meeting: "Weekly Sync #4",  date: "2025-02-17", status: "present", duration: 60  },   { id: 8,  meeting: "Investor Prep",   date: "2025-02-24", status: "present", duration: 90  },   { id: 9,  meeting: "Weekly Sync #5",  date: "2025-03-03", status: "present", duration: 60  },   { id: 10, meeting: "Team Retro",      date: "2025-03-10", status: "absent",  duration: 75  },   { id: 11, meeting: "Weekly Sync #6",  date: "2025-03-17", status: "present", duration: 60  },   { id: 12, meeting: "Annual Review",   date: "2025-03-24", status: "present", duration: 150 }, ];  // ΓöÇΓöÇ Theme Tokens ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ const THEMES = {   dark: {     bg:           "#020617",     sidebar:      "linear-gradient(180deg, #0f172a 0%, #020617 100%)",     sidebarBorder:"#1e293b",     card:         "linear-gradient(135deg, #0f172a 0%, #1e293b 100%)",     cardBorder:   "#1e293b",     surface:      "#0f172a",     surfaceBorder:"#1e293b",     text:         "#f1f5f9",     textSub:      "#94a3b8",     textMuted:    "#64748b",     textFaint:    "#475569",     navActive:    "#1e293b",     navActiveTxt: "#4ade80",     navInactiveTxt:"#64748b",     inputBg:      "#0f172a",     inputBorder:  "#1e293b",     inputText:    "#f1f5f9",     rowHover:     "#1e293b",     rowDivider:   "#0f172a",     donutTrack:   "#1e293b",     donutTxt:     "#f1f5f9",     donutSub:     "#64748b",     scrollTrack:  "#0f172a",     scrollThumb:  "#334155",     toggleBg:     "#1e293b",     toggleIcon:   "ΓÿÇ∩╕Å",     toggleLabel:  "Light",   },   light: {     bg:           "#f1f5f9",     sidebar:      "linear-gradient(180deg, #ffffff 0%, #f8fafc 100%)",     sidebarBorder:"#e2e8f0",     card:         "linear-gradient(135deg, #ffffff 0%, #f8fafc 100%)",     cardBorder:   "#e2e8f0",     surface:      "#ffffff",     surfaceBorder:"#e2e8f0",     text:         "#0f172a",     textSub:      "#334155",     textMuted:    "#64748b",     textFaint:    "#94a3b8",     navActive:    "#f0fdf4",     navActiveTxt: "#16a34a",     navInactiveTxt:"#64748b",     inputBg:      "#ffffff",     inputBorder:  "#e2e8f0",     inputText:    "#0f172a",     rowHover:     "#f8fafc",     rowDivider:   "#f1f5f9",     donutTrack:   "#e2e8f0",     donutTxt:     "#0f172a",     donutSub:     "#64748b",     scrollTrack:  "#f1f5f9",     scrollThumb:  "#cbd5e1",     toggleBg:     "#e2e8f0",     toggleIcon:   "≡ƒîÖ",     toggleLabel:  "Dark",   }, };  // ΓöÇΓöÇ Mini Bar Chart ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ function BarChart({ data, t }) {   const max = Math.max(...data.map((d) => d.value), 1);   return (     <div style={{ display: "flex", alignItems: "flex-end", gap: 6, height: 80, width: "100%" }}>       {data.map((d, i) => (         <div key={i} style={{ flex: 1, display: "flex", flexDirection: "column", alignItems: "center", gap: 4 }}>           <div style={{             width: "100%",             height: `${(d.value / max) * 72}px`,             background: "linear-gradient(180deg, #4ade80 0%, #16a34a 100%)",             borderRadius: "4px 4px 0 0",             transition: "height 0.6s cubic-bezier(.23,1,.32,1)",             minHeight: d.value > 0 ? 4 : 0,           }} />           <span style={{ fontSize: 9, color: t.textMuted, letterSpacing: "0.05em" }}>{d.label}</span>         </div>       ))}     </div>   ); }  // ΓöÇΓöÇ Donut Chart ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ function DonutChart({ percentage, t }) {   const r = 40, cx = 50, cy = 50;   const circ = 2 * Math.PI * r;   const dash = (percentage / 100) * circ;   return (     <svg viewBox="0 0 100 100" width="120" height="120">       <circle cx={cx} cy={cy} r={r} fill="none" stroke={t.donutTrack} strokeWidth="12" />       <circle         cx={cx} cy={cy} r={r} fill="none"         stroke={percentage >= 75 ? "#4ade80" : percentage >= 50 ? "#facc15" : "#f87171"}         strokeWidth="12"         strokeDasharray={`${dash} ${circ - dash}`}         strokeDashoffset={circ / 4}         strokeLinecap="round"         style={{ transition: "stroke-dasharray 1s cubic-bezier(.23,1,.32,1)" }}       />       <text x="50" y="46" textAnchor="middle" fill={t.donutTxt} fontSize="16" fontWeight="700" fontFamily="'DM Mono', monospace">         {percentage}%       </text>       <text x="50" y="60" textAnchor="middle" fill={t.donutSub} fontSize="8" fontFamily="'DM Sans', sans-serif">         ATTENDANCE       </text>     </svg>   ); }  // ΓöÇΓöÇ Stat Card ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ function StatCard({ label, value, sub, accent, t }) {   return (     <div style={{       background: t.card,       border: `1px solid ${accent}33`,       borderRadius: 16,       padding: "20px 24px",       display: "flex",       flexDirection: "column",       gap: 4,       position: "relative",       overflow: "hidden",       boxShadow: "0 1px 3px rgba(0,0,0,0.06)",     }}>       <div style={{         position: "absolute", top: 0, right: 0,         width: 80, height: 80,         background: `radial-gradient(circle, ${accent}18 0%, transparent 70%)`,         borderRadius: "0 16px 0 80px",       }} />       <span style={{ fontSize: 11, color: t.textMuted, letterSpacing: "0.1em", textTransform: "uppercase", fontFamily: "'DM Mono', monospace" }}>         {label}       </span>       <span style={{ fontSize: 32, fontWeight: 800, color: accent, fontFamily: "'DM Mono', monospace", lineHeight: 1.1 }}>         {value}       </span>       {sub && <span style={{ fontSize: 12, color: t.textFaint }}>{sub}</span>}     </div>   ); }  // ΓöÇΓöÇ Main Component ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ export default function MemberDashboard() {   const [theme, setTheme] = useState("dark");   const [tab, setTab]     = useState("dashboard");   const [filter, setFilter] = useState("all");   const [search, setSearch] = useState("");   const [user]    = useState(MOCK_USER);   const [records] = useState(MOCK_ATTENDANCE);    const t = THEMES[theme];   const isDark = theme === "dark";    // ΓöÇΓöÇ Stats   const total      = records.length;   const attended   = records.filter((r) => r.status === "present").length;   const missed     = total - attended;   const percentage = total > 0 ? Math.round((attended / total) * 100) : 0;    // ΓöÇΓöÇ Bar chart   const months  = ["Jan", "Feb", "Mar"];   const barData = months.map((label, mi) => {     const mr = records.filter((r) => new Date(r.date).getMonth() === mi);     return { label, value: mr.filter((r) => r.status === "present").length, present: true };   });    // ΓöÇΓöÇ Filtered history   const filtered = records.filter((r) => {     const mf = filter === "all" || r.status === filter;     const ms = r.meeting.toLowerCase().includes(search.toLowerCase());     return mf && ms;   });    const navItems = [     { id: "dashboard", label: "Dashboard", icon: "Γûª" },     { id: "history",   label: "History",   icon: "Γëí" },     { id: "profile",   label: "Profile",   icon: "Γùë" },   ];    return (     <div style={{ minHeight: "100vh", background: t.bg, color: t.text, fontFamily: "'DM Sans','Segoe UI',sans-serif", display: "flex", transition: "background 0.3s, color 0.3s" }}>        {/* ΓöÇΓöÇ Global styles (theme-aware) ΓöÇΓöÇ */}       <style>{`         @import url('https://fonts.googleapis.com/css2?family=DM+Mono:wght@400;500&family=DM+Sans:wght@400;500;600;700;800&display=swap');         * { box-sizing: border-box; margin: 0; padding: 0; }         ::-webkit-scrollbar { width: 4px; }         ::-webkit-scrollbar-track { background: ${t.scrollTrack}; }         ::-webkit-scrollbar-thumb { background: ${t.scrollThumb}; border-radius: 4px; }         .nav-btn  { background: none; border: none; cursor: pointer; transition: all 0.2s; }         .row-hover:hover { background: ${t.rowHover} !important; }         input::placeholder { color: ${t.textFaint}; }         input:focus { outline: none; }         .filter-btn { cursor: pointer; border: none; transition: all 0.2s; font-family: 'DM Sans',sans-serif; }         .theme-toggle { cursor: pointer; border: none; transition: all 0.25s; font-family: 'DM Sans',sans-serif; }         .theme-toggle:hover { transform: scale(1.05); }       `}</style>        {/* ΓòÉΓòÉ Sidebar ΓòÉΓòÉ */}       <aside style={{         width: 220,         background: t.sidebar,         borderRight: `1px solid ${t.sidebarBorder}`,         display: "flex",         flexDirection: "column",         padding: "32px 0",         position: "sticky",         top: 0,         height: "100vh",         transition: "background 0.3s, border-color 0.3s",       }}>          {/* Logo */}         <div style={{ padding: "0 24px 28px", borderBottom: `1px solid ${t.sidebarBorder}` }}>           <div style={{ display: "flex", alignItems: "center", gap: 10 }}>             <div style={{               width: 36, height: 36, borderRadius: 10,               background: "linear-gradient(135deg, #4ade80, #16a34a)",               display: "flex", alignItems: "center", justifyContent: "center",               fontSize: 15, fontWeight: 800, color: "#fff",             }}>FC</div>             <div>               <div style={{ fontSize: 13, fontWeight: 700, color: t.text }}>Founders Club</div>               <div style={{ fontSize: 10, color: "#16a34a", fontFamily: "'DM Mono',monospace" }}>ATTENDANCE</div>             </div>           </div>         </div>          {/* Nav */}         <nav style={{ padding: "20px 12px", flex: 1, display: "flex", flexDirection: "column", gap: 4 }}>           {navItems.map((item) => (             <button               key={item.id}               className="nav-btn"               onClick={() => setTab(item.id)}               style={{                 display: "flex", alignItems: "center", gap: 12,                 padding: "10px 14px", borderRadius: 10,                 background: tab === item.id ? t.navActive : "transparent",                 color: tab === item.id ? t.navActiveTxt : t.navInactiveTxt,                 fontSize: 14, fontWeight: tab === item.id ? 600 : 400,                 width: "100%", textAlign: "left",                 borderLeft: `3px solid ${tab === item.id ? t.navActiveTxt : "transparent"}`,               }}             >               <span style={{ fontSize: 16 }}>{item.icon}</span>               {item.label}             </button>           ))}         </nav>          {/* Theme Toggle */}         <div style={{ padding: "14px 20px", borderTop: `1px solid ${t.sidebarBorder}`, borderBottom: `1px solid ${t.sidebarBorder}` }}>           <button             className="theme-toggle"             onClick={() => setTheme(isDark ? "light" : "dark")}             style={{               display: "flex", alignItems: "center", gap: 10,               width: "100%", padding: "8px 12px", borderRadius: 10,               background: t.toggleBg,               color: t.textSub,               fontSize: 13, fontWeight: 500,             }}           >             <span style={{ fontSize: 16 }}>{t.toggleIcon}</span>             <span>{t.toggleLabel} Mode</span>             {/* pill indicator */}             <div style={{               marginLeft: "auto",               width: 32, height: 18, borderRadius: 9,               background: isDark ? "#334155" : "#bbf7d0",               position: "relative", transition: "background 0.3s",             }}>               <div style={{                 position: "absolute", top: 3,                 left: isDark ? 3 : 15,                 width: 12, height: 12, borderRadius: "50%",                 background: isDark ? "#64748b" : "#16a34a",                 transition: "left 0.25s cubic-bezier(.23,1,.32,1), background 0.3s",               }} />             </div>           </button>         </div>          {/* User mini */}         <div style={{ padding: "16px 20px" }}>           <div style={{ display: "flex", alignItems: "center", gap: 10 }}>             <div style={{               width: 32, height: 32, borderRadius: "50%",               background: "linear-gradient(135deg, #6366f1, #8b5cf6)",               display: "flex", alignItems: "center", justifyContent: "center",               fontSize: 13, fontWeight: 700, color: "#fff", flexShrink: 0,             }}>               {user.name.charAt(0)}             </div>             <div style={{ overflow: "hidden" }}>               <div style={{ fontSize: 12, fontWeight: 600, color: t.text, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }}>                 {user.name}               </div>               <div style={{ fontSize: 10, color: t.textMuted }}>{user.role}</div>             </div>           </div>         </div>       </aside>        {/* ΓòÉΓòÉ Main Content ΓòÉΓòÉ */}       <main style={{ flex: 1, padding: "40px", overflowY: "auto", transition: "background 0.3s" }}>          {/* ΓöÇΓöÇ DASHBOARD ΓöÇΓöÇ */}         {tab === "dashboard" && (           <div>             <div style={{ marginBottom: 32 }}>               <h1 style={{ fontSize: 28, fontWeight: 800, color: t.text }}>                 Good to see you, {user.name.split(" ")[0]} ≡ƒæï               </h1>               <p style={{ color: t.textMuted, marginTop: 4, fontSize: 14 }}>                 Here's your attendance overview for this cycle.               </p>             </div>              {/* Stats */}             <div style={{ display: "grid", gridTemplateColumns: "repeat(4,1fr)", gap: 16, marginBottom: 28 }}>               <StatCard label="Total Meetings" value={total}             sub="All time"                                        accent="#60a5fa" t={t} />               <StatCard label="Attended"        value={attended}          sub="Sessions present"                                accent="#4ade80" t={t} />               <StatCard label="Missed"          value={missed}            sub="Sessions absent"                                 accent="#f87171" t={t} />               <StatCard label="Attendance Rate" value={`${percentage}%`} sub={percentage >= 75 ? "Great job!" : "Keep it up"} accent={percentage >= 75 ? "#4ade80" : "#facc15"} t={t} />             </div>              {/* Charts */}             <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 20, marginBottom: 28 }}>               {/* Donut */}               <div style={{ background: t.card, border: `1px solid ${t.cardBorder}`, borderRadius: 16, padding: 28, display: "flex", alignItems: "center", gap: 28, boxShadow: "0 1px 4px rgba(0,0,0,0.06)", transition: "background 0.3s" }}>                 <DonutChart percentage={percentage} t={t} />                 <div style={{ display: "flex", flexDirection: "column", gap: 14 }}>                   <div>                     <div style={{ fontSize: 11, color: t.textMuted, letterSpacing: "0.08em", textTransform: "uppercase", fontFamily: "'DM Mono',monospace" }}>Present</div>                     <div style={{ fontSize: 22, fontWeight: 700, color: "#4ade80" }}>{attended} sessions</div>                   </div>                   <div>                     <div style={{ fontSize: 11, color: t.textMuted, letterSpacing: "0.08em", textTransform: "uppercase", fontFamily: "'DM Mono',monospace" }}>Absent</div>                     <div style={{ fontSize: 22, fontWeight: 700, color: "#f87171" }}>{missed} sessions</div>                   </div>                 </div>               </div>                {/* Bar */}               <div style={{ background: t.card, border: `1px solid ${t.cardBorder}`, borderRadius: 16, padding: 28, boxShadow: "0 1px 4px rgba(0,0,0,0.06)", transition: "background 0.3s" }}>                 <div style={{ fontSize: 11, color: t.textMuted, letterSpacing: "0.08em", textTransform: "uppercase", fontFamily: "'DM Mono',monospace", marginBottom: 16 }}>                   Monthly Sessions Attended                 </div>                 <BarChart data={barData} t={t} />               </div>             </div>              {/* Recent Activity */}             <div style={{ background: t.card, border: `1px solid ${t.cardBorder}`, borderRadius: 16, padding: 28, boxShadow: "0 1px 4px rgba(0,0,0,0.06)", transition: "background 0.3s" }}>               <div style={{ fontSize: 11, color: t.textMuted, letterSpacing: "0.08em", textTransform: "uppercase", fontFamily: "'DM Mono',monospace", marginBottom: 20 }}>                 Recent Activity               </div>               <div style={{ display: "flex", flexDirection: "column", gap: 8 }}>                 {records.slice(-5).reverse().map((r) => (                   <div key={r.id} style={{ display: "flex", alignItems: "center", gap: 14, padding: "10px 0", borderBottom: `1px solid ${t.cardBorder}` }}>                     <div style={{ width: 8, height: 8, borderRadius: "50%", background: r.status === "present" ? "#4ade80" : "#f87171", flexShrink: 0 }} />                     <div style={{ flex: 1, fontSize: 14, color: t.text, fontWeight: 500 }}>{r.meeting}</div>                     <div style={{ fontSize: 12, color: t.textMuted, fontFamily: "'DM Mono',monospace" }}>{r.date}</div>                     <div style={{                       fontSize: 11, fontWeight: 600, padding: "3px 10px", borderRadius: 20,                       background: r.status === "present" ? "#4ade8018" : "#f8717118",                       color: r.status === "present" ? "#4ade80" : "#f87171",                       textTransform: "uppercase", letterSpacing: "0.06em",                     }}>                       {r.status}                     </div>                   </div>                 ))}               </div>             </div>           </div>         )}          {/* ΓöÇΓöÇ HISTORY ΓöÇΓöÇ */}         {tab === "history" && (           <div>             <div style={{ marginBottom: 28 }}>               <h1 style={{ fontSize: 28, fontWeight: 800, color: t.text }}>Attendance History</h1>               <p style={{ color: t.textMuted, marginTop: 4, fontSize: 14 }}>Full record of all your meetings.</p>             </div>              {/* Filters row */}             <div style={{ display: "flex", gap: 12, marginBottom: 20, alignItems: "center", flexWrap: "wrap" }}>               <input                 type="text"                 placeholder="Search meetings..."                 value={search}                 onChange={(e) => setSearch(e.target.value)}                 style={{                   background: t.inputBg, border: `1px solid ${t.inputBorder}`,                   borderRadius: 10, color: t.inputText, padding: "10px 16px", fontSize: 14, width: 220,                   transition: "background 0.3s, border-color 0.3s",                 }}               />               {["all", "present", "absent"].map((f) => (                 <button                   key={f}                   className="filter-btn"                   onClick={() => setFilter(f)}                   style={{                     padding: "8px 18px", borderRadius: 10, fontSize: 13, fontWeight: 600,                     background: filter === f                       ? f === "present" ? "#4ade8022" : f === "absent" ? "#f8717122" : "#6366f122"                       : t.inputBg,                     color: filter === f                       ? f === "present" ? "#4ade80" : f === "absent" ? "#f87171" : "#818cf8"                       : t.textMuted,                     border: `1px solid ${filter === f                       ? f === "present" ? "#4ade8044" : f === "absent" ? "#f8717144" : "#6366f144"                       : t.inputBorder}`,                   }}                 >                   {f === "all" ? "All" : f === "present" ? "Γ£ô Present" : "Γ£ù Absent"}                 </button>               ))}               <span style={{ marginLeft: "auto", fontSize: 12, color: t.textMuted, fontFamily: "'DM Mono',monospace" }}>                 {filtered.length} records               </span>             </div>              {/* Table */}             <div style={{ background: t.card, border: `1px solid ${t.cardBorder}`, borderRadius: 16, overflow: "hidden", boxShadow: "0 1px 4px rgba(0,0,0,0.06)", transition: "background 0.3s" }}>               <div style={{                 display: "grid", gridTemplateColumns: "2fr 1.2fr 100px 80px",                 padding: "14px 24px", borderBottom: `1px solid ${t.cardBorder}`,                 fontSize: 11, color: t.textMuted, letterSpacing: "0.08em", textTransform: "uppercase",                 fontFamily: "'DM Mono',monospace",               }}>                 <span>Meeting</span><span>Date</span><span>Duration</span><span>Status</span>               </div>               {filtered.length === 0 ? (                 <div style={{ padding: "40px 24px", textAlign: "center", color: t.textMuted }}>                   No records match your filters.                 </div>               ) : filtered.map((r, i) => (                 <div                   key={r.id}                   className="row-hover"                   style={{                     display: "grid", gridTemplateColumns: "2fr 1.2fr 100px 80px",                     padding: "14px 24px", alignItems: "center",                     borderBottom: i < filtered.length - 1 ? `1px solid ${t.rowDivider}` : "none",                     transition: "background 0.15s",                   }}                 >                   <span style={{ fontSize: 14, color: t.text, fontWeight: 500 }}>{r.meeting}</span>                   <span style={{ fontSize: 13, color: t.textMuted, fontFamily: "'DM Mono',monospace" }}>{r.date}</span>                   <span style={{ fontSize: 13, color: t.textMuted }}>{r.duration} min</span>                   <span style={{                     display: "inline-flex", alignItems: "center", gap: 5,                     fontSize: 11, fontWeight: 600, padding: "4px 10px", borderRadius: 20,                     background: r.status === "present" ? "#4ade8018" : "#f8717118",                     color: r.status === "present" ? "#4ade80" : "#f87171",                     width: "fit-content",                   }}>                     {r.status === "present" ? "Γ£ô" : "Γ£ù"} {r.status}                   </span>                 </div>               ))}             </div>           </div>         )}          {/* ΓöÇΓöÇ PROFILE ΓöÇΓöÇ */}         {tab === "profile" && (           <div>             <div style={{ marginBottom: 28 }}>               <h1 style={{ fontSize: 28, fontWeight: 800, color: t.text }}>My Profile</h1>               <p style={{ color: t.textMuted, marginTop: 4, fontSize: 14 }}>Your account details and attendance summary.</p>             </div>              <div style={{ display: "grid", gridTemplateColumns: "360px 1fr", gap: 20 }}>               {/* Profile card */}               <div style={{ background: t.card, border: `1px solid ${t.cardBorder}`, borderRadius: 20, padding: 32, display: "flex", flexDirection: "column", alignItems: "center", gap: 16, boxShadow: "0 1px 4px rgba(0,0,0,0.06)", transition: "background 0.3s" }}>                 <div style={{                   width: 80, height: 80, borderRadius: "50%",                   background: "linear-gradient(135deg, #6366f1, #8b5cf6)",                   display: "flex", alignItems: "center", justifyContent: "center",                   fontSize: 32, fontWeight: 800, color: "#fff",                   boxShadow: "0 0 0 4px #6366f122, 0 0 0 8px #6366f10a",                 }}>                   {user.name.charAt(0)}                 </div>                 <div style={{ textAlign: "center" }}>                   <div style={{ fontSize: 20, fontWeight: 700, color: t.text }}>{user.name}</div>                   <div style={{ fontSize: 13, color: t.textMuted, marginTop: 4 }}>{user.email}</div>                 </div>                 <div style={{                   background: "#6366f118", color: "#818cf8", fontSize: 11, fontWeight: 600,                   padding: "4px 14px", borderRadius: 20, letterSpacing: "0.08em",                   textTransform: "uppercase", border: "1px solid #6366f133",                 }}>                   {user.role}                 </div>                 <div style={{ width: "100%", borderTop: `1px solid ${t.cardBorder}`, paddingTop: 20, display: "flex", flexDirection: "column", gap: 12 }}>                   {[{ label: "Member ID", value: user.id }, { label: "Joined", value: user.joinedAt }].map(({ label, value }) => (                     <div key={label} style={{ display: "flex", justifyContent: "space-between", alignItems: "center" }}>                       <span style={{ fontSize: 12, color: t.textMuted }}>{label}</span>                       <span style={{ fontSize: 12, color: t.textSub, fontFamily: "'DM Mono',monospace" }}>{value}</span>                     </div>                   ))}                 </div>               </div>                {/* Stats side */}               <div style={{ display: "flex", flexDirection: "column", gap: 16 }}>                 <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16 }}>                   <StatCard label="Total Meetings"  value={total}             accent="#60a5fa" sub="All time"                                             t={t} />                   <StatCard label="Attendance Rate" value={`${percentage}%`} accent={percentage >= 75 ? "#4ade80" : "#facc15"} sub={percentage >= 75 ? "Excellent" : "Needs improvement"} t={t} />                   <StatCard label="Present"         value={attended}          accent="#4ade80" sub="Sessions" t={t} />                   <StatCard label="Missed"          value={missed}            accent="#f87171" sub="Sessions" t={t} />                 </div>                  {/* Standing banner */}                 <div style={{                   background: percentage >= 75                     ? isDark ? "linear-gradient(135deg,#14532d22,#166534aa)" : "linear-gradient(135deg,#dcfce7,#bbf7d0)"                     : isDark ? "linear-gradient(135deg,#7c2d1222,#92400e88)" : "linear-gradient(135deg,#fff7ed,#fed7aa)",                   border: `1px solid ${percentage >= 75 ? "#4ade8033" : "#fb923c33"}`,                   borderRadius: 16, padding: "20px 24px",                   display: "flex", alignItems: "center", gap: 16,                   transition: "background 0.3s",                 }}>                   <div style={{ fontSize: 32 }}>{percentage >= 75 ? "≡ƒÅå" : "≡ƒôî"}</div>                   <div>                     <div style={{ fontSize: 15, fontWeight: 700, color: t.text }}>                       {percentage >= 75 ? "Great Standing!" : "Attendance Alert"}                     </div>                     <div style={{ fontSize: 13, color: t.textMuted, marginTop: 2 }}>                       {percentage >= 75                         ? "You're above the 75% threshold. Keep it up!"                         : "You're below 75%. Try not to miss upcoming meetings."}                     </div>                   </div>                 </div>               </div>             </div>           </div>         )}       </main>     </div>   ); }